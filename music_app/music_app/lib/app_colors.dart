import 'package:flutter/material.dart';

class AppColors {
  // Background Colors
  static const Color primaryBackground =Color(0xFF110929);
  static const Color secondaryBackground = Color(0xFF1F1F1F);

  // Accent / Primary Colors
  static const Color accentPurple = Color(0xFF9146FF);

  // Text Colors
  static const Color textWhite = Colors.white;
  static const Color textGrey = Colors.white70;

  // Tab / Inactive Colors
  static const Color inactiveTab = Colors.grey;

  // Search Bar Color
  static const Color searchBar = Color(0xFF2C2C2C); // a bit lighter for contrast

  // Artist / Category Card Backgrounds
  static const Color artistCardBackground = Color(0xFF1F1F1F);
  static const Color categoryCardBackground = Color(0xFF2C2C2C);

  // Miscellaneous
  static const Color iconColor = Colors.white;
  static const Color borderColor = Colors.white24;
}
